# Local environment
# Configure your own database credentials
# Coonnect your database (data source in Eclipse, local plsql, heroku pg:psql). Suggest via web for heroku
# execute initTablePersons.sql
# execute initData.sql

jdbc.driverClassName=org.postgresql.Driver

# EDIT YOUR DATABASE CREDENTIALS HERE
# for heroku jdbc:postgresql://host:5432/postgres?user=user&password=pass&sslmode=require
# check data with heroku pg:credentials:url
jdbc.url=jdbc:postgresql://localhost:5432/postgres
jdbc.username=postgres
jdbc.password=1234
hibernate.dialect=org.hibernate.dialect.MySQLDialect


jpa.database = POSTGRESQL
hibernate.generate_statistics = true
hibernate.show_sql = true
jpa.showSql = true
jpa.generateDdl = true
